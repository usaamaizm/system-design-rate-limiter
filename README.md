# system-design-rate-limiter

Learning repo: rate limiter implementations in Java Spring Boot, from naive fixed window to production-ready designs.

## Steps
- Step 0: Global Fixed Window (naive)
- Step 1: Per-IP Fixed Window (in-memory)

- Step 1.1: Add RateLimitDecision + headers
